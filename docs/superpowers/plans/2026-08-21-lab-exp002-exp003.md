# /lab EXP-002 tonight's tides + EXP-003 dream decay — 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 补满首发三件展品的后两件。EXP-002 tonight's tides(查询性仪器:输入就寝时间,90 分钟睡眠周期化作潮汐波形铺开,浅睡窗口标绿)与 EXP-003 dream decay(反转 EncryptedText:写下一段梦,每次重访即一次复述,自动衰减、不可逆)。两个判断点已由用户于 2026-08-21 确认:EXP-003 维持自动衰减触发(不做手动重读按钮);梦境文字维持 Geist Sans(不给 Lora 开第三处例外)。

**Architecture:** /lab 框架(索引、详情路由、注册表、HUD)已在 Phase 2 建好且**本期零改动**——`lib/experiments.ts` 的 REGISTRY、`components/lab/lab-grid.tsx`、`components/lab/experiment-hud.tsx`、`app/lab/[slug]/page.tsx` 全部已经是"任意实验通用"的形态,新增实验只需要:一份 `content/lab/<slug>/index.md` + 一个 `components/experiments/<slug>.tsx` + REGISTRY 里一行映射 + 一张海报 SVG。两件实验都是纯 client 组件、v1 均静音(SOUND-DESIGN §4 已定案),`lib/audio.ts` 本期不碰。

**Tech Stack:** 与 Phase 2 一致——Next.js 16(app router, static export)、React 19、TypeScript strict、Tailwind v4、`simplex-noise`(已安装,EXP-002 复用)。EXP-003 的输入框**不引入新依赖**:COMPONENT-SOURCES.md 曾为它预留 Aceternity 的 Placeholders And Vanish Input,但那个组件的确切 prop 签名此刻无法核实("不许假"红线:宁可不装,不猜一个可能装不上的 API)。本计划用一个自写的 textarea + CSS 淡出实现同等功能,verified 可跑;如果实现时你有网络且想要更华丽的蒸发动效,可以另装该组件替换输入这一小块,不影响其余部分。

## Global Constraints

- **本期不新增任何 Next.js 路由/API 面**——不碰 `generateStaticParams`、不加动态路由、不碰 Route Handler。Phase 2 计划里核对过的 Next 16 静态导出硬约束(`params` 是 Promise、`dynamicParams=false`、`next/dynamic ssr:false` 只能在 client 模块等)本期用不上新的,不必重新核对文档。
- **包管理用 pnpm**(`pnpm-lock.yaml` + `packageManager: pnpm@11`)。不要动 `package-lock.json`。
- **单一强调色:** 终端绿 `#22c55e`(Tailwind `green-500`)。灰阶只用 `white / white/70 / white/40`(交互 mono 用 `white/60`)。两个实验都不引入第二种彩色——EXP-003 的乱码字符用同一套灰阶,不做"衰减=变红"这类额外语义色。
- **仪表读数规格(DESIGN §2):** 一切"报数"用 `font-mono text-xs text-white/40`,无字号/粗细阶。相位词、`bedtime`、`reread N times` 这类实验内读数按 §7.1 硬编码英文,**不进 `lib/i18n/strings.ts`**——这条 Phase 2 已确立的先例本期沿用,两个实验都不需要修改 i18n 字符串表。
- **声音三法则:** 两件实验 v1 均静音(SOUND-DESIGN §4 已定案)。EXP-003 的静音**是内容**,记录面板 observation 栏原样写"遗忘本身不发出任何声音"这句话,不是留白。
- **编号永不复用;** 实验状态只有 `ongoing | archived`。
- **不许假(本期最直接相关的两条):** EXP-002 的 observation 必须包含"这是启发式模型,不是个人实测数据"的诚实条款;EXP-003 的衰减必须真实可复现(多次刷新验证确实在丢词),不能是视觉上演一次就不变的假动画。
- **本仓库没有测试框架**,本期不引入。衰减算法、周期窗口计算都写成纯函数(`decayOnce` / `computeWindows`),日后要补测试可以直接对着它们写,不用改调用方。
- **验收 = `npx tsc --noEmit` + `pnpm build:willsleep`**(涉及 sitemap/门牌收录时再跑 `pnpm build:yueqiao`,但本期两者都不碰,`lab.open` 早已是 `true`,`app/sitemap.ts` 已经通用地遍历 `getLabExperiments()`——新内容自动收录,不用改这两个文件)。
- **iCloud 构建隐患:** build 卡住先怀疑 iCloud dataless 文件(memory `icloud-drive-build-hazard`)。
- **提交:** 每个任务收尾单独 commit,`feat:` 前缀,结尾带 `Co-Authored-By: Claude <noreply@anthropic.com>`。

---

## 文件结构(本计划结束时)

**新建:**
- `content/lab/002-tonight-tides/index.md` — EXP-002 元数据与记录面板文字。
- `content/lab/003-dream-decay/index.md` — EXP-003 元数据与记录面板文字。
- `components/experiments/tonight-tides.tsx` — EXP-002 canvas 潮汐场 + bedtime 控件 + 醒来时间读数。
- `components/experiments/dream-decay.tsx` — EXP-003 vanish 输入 + 衰减状态机 + 字符抖动渲染。
- `public/lab/002-tonight-tides.svg`、`public/lab/003-dream-decay.svg` — 静态海报。

**修改:**
- `lib/experiments.ts` — REGISTRY 新增两行映射。

**不碰:** `lib/content.ts`(`getLabExperiments`/`getLabExperiment` 已经通用扫描 `content/lab/*`,新文件夹自动被发现)、`lib/rooms.ts`、`app/sitemap.ts`、`app/lab/page.tsx`、`components/lab/lab-grid.tsx`、`components/lab/experiment-hud.tsx`、`app/lab/[slug]/page.tsx`、`lib/audio.ts`、`lib/i18n/strings.ts`——这是"本期壳层零改动"的字面意思。

---

## Task 1: 内容层 — 两份实验元数据

**Files:**
- Create: `content/lab/002-tonight-tides/index.md`
- Create: `content/lab/003-dream-decay/index.md`

**Interfaces:**
- Consumes: 无新接口,复用 Phase 2 已定的 `LabExperiment` frontmatter 形状(`exp/title/question/status/poster/method/observation/instruments`,见 `lib/content.ts` 的 `toLabExperiment`)。
- Produces: `getLabExperiments()` 之后会返回 3 条(001/002/003),走廊门缝读数 `lab: 3 experiments · 3 ongoing` 自动生效,零代码改动(`getRoomStatuses().lab` 已经是通用计数)。

- [ ] **Step 1: 写 `content/lab/002-tonight-tides/index.md`**

```markdown
---
exp: "002"
title: tonight's tides
question: what time will I wake up tonight, least badly?
status: ongoing
poster: /lab/002-tonight-tides.svg
method: >-
  就寝时间起横向铺开 9 小时,每 90 分钟一个睡眠周期(含 14 分钟入睡缓冲),
  振幅按周期深浅曲线走——第一个周期最深,之后逐周期变浅、REM 占比上升。
  浅睡窗口是边界 ±10 分钟的区间高亮,不是精确的单点;bedtime 默认取此刻
  本地时间("如果现在上床"),数字本身可用方向键微调、点击后可键入覆盖。
observation: >-
  这是基于人群平均 90 分钟周期的启发式模型,不是这位访客的个人睡眠实测
  数据;真实周期时长因人而异、因夜而异(压力、酒精、时差都会改变它)。
instruments:
  - simplex-noise(潮汐场,与 EXP-001 同源手法、不同参数化)
  - canvas 2d
---
```

- [ ] **Step 2: 写 `content/lab/003-dream-decay/index.md`**

```markdown
---
exp: "003"
title: dream decay
question: at which retelling does a dream go missing?
status: ongoing
poster: /lab/003-dream-decay.svg
method: >-
  写下一段梦,只存这台浏览器的 localStorage,不上传、不联网。每次重新
  打开这个实验页就是一次复述,自动触发一次衰减:约 15% 的词变成乱码
  或彻底消失,下限是原文 20% 的词仍然可读,首句保留一个锚点词。衰减
  不可逆、也不可被叫停——遗忘不是访客能主动选择要不要发生的事。
observation: >-
  遗忘本身不发出任何声音。这也是内容,不是留白。
instruments:
  - localStorage(仅本地,不上传、不联网)
  - 字符抖动(反向驱动 encrypted-text.tsx 的技术:清晰→乱码,不是乱码→清晰)
---
```

- [ ] **Step 3: 类型检查 + 构建**

Run: `npx tsc --noEmit`
Expected: 无错误。
Run: `pnpm build:willsleep`
Expected: 构建成功;`/lab/002-tonight-tides/` 与 `/lab/003-dream-decay/` 已可静态生成(HUD 面板文字齐全),但页面内的实验画布此刻是空白——REGISTRY 还没接上(Task 2/3 才接),这是预期中的中间状态,不是 bug。
Run: `pnpm dev` → 打开 `http://localhost:3000/lab/`
Expected: 三张卡片(EXP-001/002/003),后两张暂时没有海报图(Step 里的 svg 还没建),文字齐全。

- [ ] **Step 4: Commit**

```bash
git add content/lab/002-tonight-tides content/lab/003-dream-decay
git commit -m "feat(lab): content metadata for EXP-002 tonight's tides + EXP-003 dream decay"
```

---

## Task 2: EXP-002 仪器 — tonight-tides.tsx

**Files:**
- Create: `components/experiments/tonight-tides.tsx`
- Create: `public/lab/002-tonight-tides.svg`
- Modify: `lib/experiments.ts`(REGISTRY 新增一行)

**Interfaces:**
- Consumes: `createNoise3D`(simplex-noise,已安装)。**不碰 `@/lib/audio`**——本实验静音。
- Produces: `export default function TonightTides()`——无 props,经 `lib/experiments.ts` 的 `next/dynamic(ssr:false)` 载入。

**设计要点(SPEC §2,反默认——不是睡眠计算器 App 的折线图/图例/原生 time input):**

canvas 的 x 轴是"就寝后经过的分钟数"(0–540min = 9h),**不是钟表时间**——这一点决定了潮汐场的形状与 bedtime 取值完全无关,只有 x 轴上叠加的醒来时间标签(真实 DOM 文本,不锁进 canvas)才把"分钟偏移"换算成"几点几分"。好处:调整 bedtime 时画布不需要重绘,只有文字标签重新计算,reduced-motion 降级因此格外简单——画布本来就是静态的时间无关形状,只在窗口尺寸变化时重绘一次。

- [ ] **Step 1: 写 `components/experiments/tonight-tides.tsx`**

```tsx
"use client";

// EXP-002 tonight's tides(§DESIGN 5 /lab,2026-08-21 spec §2)。
// 查询性仪器,不是过程性仪器(与 EXP-001 的区别见 spec):访客带着一个具体
// 问题来,读几个数字就走。复用 EXP-001 验证过的 simplex-noise 场绘制手法,
// 参数化方式完全不同——振幅按睡眠周期深浅曲线走,不是呼吸包络。
//
// 关键简化:canvas 的 x 轴是"就寝后经过的分钟数",与 bedtime 实际取值无关;
// 只有醒来时间标签(真实 DOM 文本)把分钟偏移换算成钟表时间。这意味着
// bedtime 变化时画布不必重绘,reduced-motion 降级不需要特殊的"重绘一帧"
// 逻辑——它本来就是一帧静态形状,只在窗口尺寸变化时重画。

import { useEffect, useRef, useState } from "react";
import { createNoise3D } from "simplex-noise";

const ONSET_BUFFER_MIN = 14; // 入睡缓冲(SPEC §2,常见简化模型)
const CYCLE_MIN = 90;
const WINDOW_HALF_MIN = 10; // 浅睡窗口是区间,±10min,不是精确单点(SPEC §2)
const SPAN_MIN = 9 * 60; // 铺开范围:bedtime 起 +9h

const WAVE_COLORS = ["#22c55e", "#4ade80", "#16a34a", "#86efac", "#15803d"];
const HIGHLIGHT = "#22c55e"; // 全站唯一强调色(§DESIGN 2)

type SleepWindow = {
  cycle: number;
  centerMin: number;
  startMin: number;
  endMin: number;
  /** 1 = 最深(第 1 周期),每周期 -0.15,下限 0.3——启发式简化,不是医学模型(诚实条款见 content)。 */
  depth: number;
};

/** 纯函数:bedtime 起 spanMin 分钟内的浅睡窗口。可脱离组件单独测试。 */
function computeWindows(spanMin: number = SPAN_MIN): SleepWindow[] {
  const windows: SleepWindow[] = [];
  for (let cycle = 1; ; cycle++) {
    const centerMin = ONSET_BUFFER_MIN + cycle * CYCLE_MIN;
    if (centerMin - WINDOW_HALF_MIN > spanMin) break;
    windows.push({
      cycle,
      centerMin,
      startMin: Math.max(0, centerMin - WINDOW_HALF_MIN),
      endMin: Math.min(spanMin, centerMin + WINDOW_HALF_MIN),
      depth: Math.max(0.3, 1 - (cycle - 1) * 0.15),
    });
  }
  return windows;
}

// 模块级常量,输入恒定,算一次够用——组件里直接引用,不进任何 effect 依赖数组。
const WINDOWS = computeWindows();

/** 深浅曲线在任意分钟点的插值(用于 amplitude),窗口之间线性过渡,不跳变。 */
function depthAt(min: number): number {
  let prev = { centerMin: 0, depth: 1 };
  for (const win of WINDOWS) {
    if (min <= win.centerMin) {
      const span = win.centerMin - prev.centerMin || 1;
      const t = (min - prev.centerMin) / span;
      return prev.depth + (win.depth - prev.depth) * t;
    }
    prev = { centerMin: win.centerMin, depth: win.depth };
  }
  return prev.depth;
}

const nowMinutes = () => {
  const d = new Date();
  return d.getHours() * 60 + d.getMinutes();
};

const formatHM = (totalMin: number) => {
  const m = ((Math.round(totalMin) % 1440) + 1440) % 1440;
  return `${String(Math.floor(m / 60)).padStart(2, "0")}:${String(m % 60).padStart(2, "0")}`;
};

/** "23:30" / "2330" / "23 30" → 分钟数;解析失败返回 null(键入覆盖,SPEC §2)。 */
function parseHM(raw: string): number | null {
  const match = raw.trim().match(/^(\d{1,2})[:\s]?(\d{2})$/);
  if (!match) return null;
  const h = Number(match[1]);
  const mm = Number(match[2]);
  if (h > 23 || mm > 59) return null;
  return h * 60 + mm;
}

const prefersReducedMotion = () =>
  typeof window !== "undefined" &&
  window.matchMedia("(prefers-reduced-motion: reduce)").matches;

export default function TonightTides() {
  const canvasRef = useRef<HTMLCanvasElement>(null);
  const containerRef = useRef<HTMLDivElement>(null);
  // ssr:false,挂载即在 client。默认值 = 此刻本地时间("如果现在上床"),
  // 不是空表单——呼应仪器随时汇报真实状态的身份(HOME §4.6 同源判断)。
  const [bedtime, setBedtime] = useState<number>(() => nowMinutes());
  const [editing, setEditing] = useState(false);
  const [draft, setDraft] = useState("");

  const adjustMinutes = (delta: number) =>
    setBedtime((m) => ((m + delta) % 1440 + 1440) % 1440);

  const commitDraft = () => {
    const parsed = parseHM(draft);
    if (parsed !== null) setBedtime(parsed);
    setEditing(false);
  };

  // 潮汐场渲染:x 轴与 bedtime 无关(见文件头注释),只在挂载与 resize 时跑。
  // reduced-motion 时不起 rAF,只画一帧——不需要因 bedtime 变化而重绘。
  useEffect(() => {
    const canvas = canvasRef.current;
    const container = containerRef.current;
    if (!canvas || !container) return;
    const ctx = canvas.getContext("2d");
    if (!ctx) return;
    const noise = createNoise3D();
    const reduced = prefersReducedMotion();

    let w = 0;
    let h = 0;
    let raf = 0;

    const xForMinute = (min: number) => (min / SPAN_MIN) * w;

    const draw = (nt: number) => {
      ctx.filter = "blur(6px)";
      ctx.fillStyle = "#000";
      ctx.globalAlpha = 1;
      ctx.fillRect(0, 0, w, h);

      // 浅睡窗口高亮:区间背景带在波形下面(单一强调色,§DESIGN 2)。
      for (const win of WINDOWS) {
        ctx.globalAlpha = 0.12;
        ctx.fillStyle = HIGHLIGHT;
        ctx.fillRect(
          xForMinute(win.startMin),
          0,
          xForMinute(win.endMin) - xForMinute(win.startMin),
          h,
        );
      }

      ctx.lineWidth = 3;
      for (let i = 0; i < WAVE_COLORS.length; i++) {
        ctx.beginPath();
        ctx.strokeStyle = WAVE_COLORS[i];
        ctx.globalAlpha = 0.5;
        for (let x = 0; x <= w; x += 4) {
          const min = (x / w) * SPAN_MIN;
          const amp = 10 + 46 * depthAt(min);
          const y = h * 0.6 + noise(x / 260, 0.3 * i, nt + i * 0.6) * amp;
          ctx.lineTo(x, y);
        }
        ctx.stroke();
      }
      ctx.filter = "none";
    };

    const resize = () => {
      w = canvas.width = container.clientWidth;
      h = canvas.height = container.clientHeight;
      draw(2.5); // 固定相位的静态帧,resize 与 reduced-motion 共用
    };

    const frame = (now: number) => {
      draw((now / 1000) * 0.08); // 缓慢漂移——潮水没有停(策展三问③)
      raf = requestAnimationFrame(frame);
    };

    resize();
    window.addEventListener("resize", resize);
    if (!reduced) raf = requestAnimationFrame(frame);

    return () => {
      cancelAnimationFrame(raf);
      window.removeEventListener("resize", resize);
    };
  }, []);

  return (
    <div ref={containerRef} className="absolute inset-0 overflow-hidden bg-black">
      <canvas ref={canvasRef} className="absolute inset-0 h-full w-full" />

      {/* bedtime 控件:mono 大字读数本身即控件,不用原生 time input(SPEC §2 反默认——
          原生 chrome 会击穿"无第二种彩色、无 chrome 泄漏"的纪律)。 */}
      <div className="pointer-events-none absolute inset-x-0 top-0 flex items-center justify-center p-8">
        <div className="pointer-events-auto flex items-center gap-3 font-mono">
          <span className="text-xs text-white/40">bedtime</span>
          {editing ? (
            <input
              autoFocus
              value={draft}
              onChange={(e) => setDraft(e.target.value)}
              onBlur={commitDraft}
              onKeyDown={(e) => {
                if (e.key === "Enter") commitDraft();
                if (e.key === "Escape") setEditing(false);
              }}
              aria-label="type bedtime, e.g. 23:30"
              className="w-20 border-b border-green-500/40 bg-transparent text-2xl text-white outline-none focus-visible:border-green-500"
            />
          ) : (
            <button
              type="button"
              onClick={() => {
                setDraft(formatHM(bedtime));
                setEditing(true);
              }}
              onKeyDown={(e) => {
                if (e.key === "ArrowUp") {
                  e.preventDefault();
                  adjustMinutes(1);
                }
                if (e.key === "ArrowDown") {
                  e.preventDefault();
                  adjustMinutes(-1);
                }
              }}
              aria-label={`bedtime ${formatHM(bedtime)}, arrow keys adjust by the minute, click to type`}
              className="text-2xl text-white underline-offset-8 transition-colors duration-200 hover:underline focus-visible:underline"
            >
              {formatHM(bedtime)}
            </button>
          )}
        </div>
      </div>

      {/* 醒来候选时间:真实 DOM 文本,可选中、可被读屏,不锁进 canvas(SPEC §2)。 */}
      <div className="pointer-events-none absolute inset-0">
        {WINDOWS.map((win) => (
          <span
            key={win.cycle}
            className="absolute bottom-6 -translate-x-1/2 whitespace-nowrap font-mono text-xs text-white/60"
            style={{ left: `${(win.centerMin / SPAN_MIN) * 100}%` }}
          >
            {formatHM(bedtime + win.centerMin)}
          </span>
        ))}
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 写 `public/lab/002-tonight-tides.svg`(静态海报,加一条高亮带区别于 001)**

```svg
<svg xmlns="http://www.w3.org/2000/svg" width="800" height="450" viewBox="0 0 800 450" role="img" aria-label="tonight's tides">
  <rect width="800" height="450" fill="#000"/>
  <defs>
    <filter id="b2" x="-20%" y="-20%" width="140%" height="140%">
      <feGaussianBlur stdDeviation="6"/>
    </filter>
  </defs>
  <rect x="330" y="0" width="70" height="450" fill="#22c55e" opacity="0.14"/>
  <g filter="url(#b2)" fill="none" stroke-linecap="round">
    <path d="M0 260 C 100 240, 200 270, 300 250 S 500 200, 600 230 S 760 260, 800 245" stroke="#22c55e" stroke-width="22" opacity="0.5"/>
    <path d="M0 250 C 120 220, 240 260, 360 235 S 600 195, 800 225" stroke="#4ade80" stroke-width="18" opacity="0.35"/>
    <path d="M0 270 C 140 255, 280 280, 420 260 S 680 220, 800 255" stroke="#16a34a" stroke-width="20" opacity="0.3"/>
  </g>
</svg>
```

- [ ] **Step 3: 在 `lib/experiments.ts` 新增注册**

在现有 `BreathingField` 下面加一行 dynamic import,并把它加进 `REGISTRY`:

```typescript
const TonightTides = dynamic(() => import("@/components/experiments/tonight-tides"), {
  ssr: false,
});

const REGISTRY: Record<string, ComponentType> = {
  "001-breathing-field": BreathingField,
  "002-tonight-tides": TonightTides,
};
```

- [ ] **Step 4: 类型检查 + 构建 + 手动验证**

Run: `npx tsc --noEmit`
Expected: 无错误。
Run: `pnpm build:willsleep`
Expected: 构建成功。
Run: `pnpm dev` → 打开 `http://localhost:3000/lab/002-tonight-tides/`
Expected: 全屏绿色潮汐场缓慢漂移;顶部居中 `bedtime · HH:MM`(默认此刻本地时间);5 个浅睡窗口(绿色高亮带)沿时间轴分布,每条带底部有对应的醒来时间 mono 标签;点击 bedtime 数字可键入新值(如 `2330` 或 `23:30`)覆盖,回车生效、Esc 取消;聚焦数字按钮时 ↑↓ 能以分钟微调,标签跟着实时变化;开系统"减弱动态效果"后潮汐场为静止帧,bedtime 仍可调整(标签更新,画面不变——这是设计,不是降级不完整);左上徽标点开四栏面板,observation 栏含"启发式模型"诚实条款字样。

- [ ] **Step 5: Commit**

```bash
git add components/experiments/tonight-tides.tsx public/lab/002-tonight-tides.svg lib/experiments.ts
git commit -m "feat(lab): EXP-002 tonight's tides — sleep-cycle tide field, bedtime readout control"
```

---

## Task 3: EXP-003 仪器 — dream-decay.tsx

**Files:**
- Create: `components/experiments/dream-decay.tsx`
- Create: `public/lab/003-dream-decay.svg`
- Modify: `lib/experiments.ts`(REGISTRY 再新增一行)

**Interfaces:**
- Consumes: 无外部依赖(不碰 `@/lib/audio`;不装新组件库,理由见计划头部)。
- Produces: `export default function DreamDecay()`——无 props,经 `next/dynamic(ssr:false)` 载入。

**设计要点(SPEC §3,已确认的两个判断:自动衰减触发 + Geist Sans):**

状态机 `empty → written(→ 再次挂载即衰减一次)`。**分词对中英混排安全**:CJK 字符逐字一个词(中文没有空格分词,这是唯一不依赖分词库/网络的诚实做法);非 CJK 按空白切分,标点跟前一个词一起走,不单独成词——这是"约 15% 的词"这种口语级计数下可接受的近似,不追求语言学精确。**衰减是两段式**(`intact → scrambled → gone`):同一个词第一次被选中变乱码,第二次被选中才彻底消失——这带来一个我在写这份计划时才注意到的、和 spec 文字描述一致但值得指出的性质:**第一次复述只会让词变模糊,不会立刻丢任何一个词**,只有词已经模糊过一次、又被再次选中,才真的消失。这比"每次都可能直接抹掉一个从没模糊过的词"更像真实的遗忘曲线,建议保留这个设计,不要为了"更快看到效果"改成一段式。

- [ ] **Step 1: 写 `components/experiments/dream-decay.tsx`**

```tsx
"use client";

// EXP-003 dream decay(§DESIGN 5 /lab,2026-08-21 spec §3,用户已确认两点:
// ① 复述触发方式维持自动——打开页面就算一次复述,不做手动"重读"按钮;
// ② 梦境文字维持 Geist Sans,不给 Lora 开第三处例外)。
//
// 反转 EncryptedText:清晰→乱码,不是乱码→清晰。写下一段梦,仅存本地
// localStorage(不上传、不联网,静态站物理上也做不到)。每次重新打开这个
// 实验页就是一次"复述",mount 时自动触发一次衰减——遗忘不是访客能叫停
// 的事,做成按钮会让"这件仪器研究的问题"不成立(spec §3 的论证)。
//
// 衰减两段式:intact → scrambled → gone。同一个词第一次被选中只变乱码,
// 第二次被选中才真的消失——所以第一次复述只会让文字变模糊,不会立刻丢
// 任何一个词,更接近真实遗忘的渐进感。

import { useEffect, useState } from "react";

const STORAGE_KEY = "sl.exp003.dream";
const DECAY_FRACTION = 0.15; // 每次复述约 15% 的原词(SPEC §3)
const FLOOR_FRACTION = 0.2; // 至少 20% 原词保持"可读"(intact 或 scrambled)
const SCRAMBLE_CHARS = "abcdefghijklmnopqrstuvwxyz···⋯⌇∷⁘";
// 中文语境下也用拉丁乱码字符(与走廊开场解密美学同源,§DESIGN 10.1),
// 不伪造"乱码汉字"——那会显得像编码错误而不是遗忘。

// CJK 字符逐字一个词;非 CJK 按空白切分,标点随前一个词。
// 中文没有空格分词,这是唯一不依赖分词库/网络的诚实做法(不许假)。
const CJK_RE = /[一-鿿぀-ヿ가-힯]/;

type WordState = "intact" | "scrambled" | "gone";
type Token = { text: string; isWord: boolean };
type DecayWord = Token & { state: WordState };

type StoredRecord = {
  words: DecayWord[]; // 当前快照,有损、不存原文(SPEC §3:衰减是有损单向的)
  decayCount: number;
  anchorIndex: number; // 首句锚点词,永不进入衰减候选
};

function tokenize(text: string): Token[] {
  const tokens: Token[] = [];
  let buf = "";
  const flush = () => {
    if (buf) tokens.push({ text: buf, isWord: true });
    buf = "";
  };
  for (const ch of text) {
    if (/\s/.test(ch)) {
      flush();
      tokens.push({ text: ch, isWord: false });
    } else if (CJK_RE.test(ch)) {
      flush();
      tokens.push({ text: ch, isWord: true });
    } else {
      buf += ch;
    }
  }
  flush();
  return tokens;
}

function firstWordIndex(tokens: Token[]): number {
  const i = tokens.findIndex((t) => t.isWord);
  return i === -1 ? 0 : i;
}

/** 纯函数:一次复述的衰减。seed 可注入,便于日后写确定性测试。 */
function decayOnce(
  words: DecayWord[],
  anchorIndex: number,
  seed: () => number = Math.random,
): DecayWord[] {
  const total = words.filter((w) => w.isWord).length;
  const goneNow = words.filter((w) => w.isWord && w.state === "gone").length;
  const floor = Math.ceil(total * FLOOR_FRACTION);
  const maxNewGone = Math.max(0, total - floor - goneNow);
  const quota = Math.min(Math.round(total * DECAY_FRACTION), total);
  if (quota <= 0) return words;

  const candidates = words
    .map((w, i) => ({ i, state: w.state, isWord: w.isWord }))
    .filter((c) => c.isWord && c.i !== anchorIndex && c.state !== "gone");

  // Fisher–Yates,注入的 seed 便于将来写确定性测试。
  const shuffled = [...candidates];
  for (let i = shuffled.length - 1; i > 0; i--) {
    const j = Math.floor(seed() * (i + 1));
    [shuffled[i], shuffled[j]] = [shuffled[j], shuffled[i]];
  }

  let goneUsed = 0;
  const next = words.map((w) => ({ ...w }));
  for (const c of shuffled.slice(0, quota)) {
    if (next[c.i].state === "intact") {
      next[c.i].state = "scrambled";
    } else if (next[c.i].state === "scrambled" && goneUsed < maxNewGone) {
      next[c.i].state = "gone";
      goneUsed++;
    }
    // 已到下限:该词维持 scrambled,不越线(衰减有下限,SPEC §3)。
  }
  return next;
}

function scrambleFor(text: string): string {
  return Array.from(text)
    .map(() => SCRAMBLE_CHARS[Math.floor(Math.random() * SCRAMBLE_CHARS.length)])
    .join("");
}

function loadRecord(): StoredRecord | null {
  if (typeof window === "undefined") return null;
  try {
    const raw = window.localStorage.getItem(STORAGE_KEY);
    if (!raw) return null;
    const parsed = JSON.parse(raw) as StoredRecord;
    return Array.isArray(parsed.words) ? parsed : null;
  } catch {
    return null;
  }
}

function saveRecord(record: StoredRecord): void {
  try {
    window.localStorage.setItem(STORAGE_KEY, JSON.stringify(record));
  } catch {
    // 存储不可用(隐私模式等)——诚实降级:这次不记住,不报错打断体验。
  }
}

/** 单个词的渲染:justChanged 时先抖动几帧乱码,再定格到目标态(SPEC §3:变化要能被看见)。 */
function DecayWordSpan({
  word,
  justChanged,
}: {
  word: DecayWord;
  justChanged: boolean;
}) {
  const [display, setDisplay] = useState(
    justChanged ? word.text : word.state === "gone" ? "" : word.text,
  );

  useEffect(() => {
    if (!justChanged) return;
    let frame = 0;
    const maxFrames = 8;
    const id = window.setInterval(() => {
      frame++;
      if (frame >= maxFrames) {
        window.clearInterval(id);
        setDisplay(word.state === "gone" ? "" : scrambleFor(word.text));
        return;
      }
      setDisplay(scrambleFor(word.text));
    }, 70);
    return () => window.clearInterval(id);
    // 只在挂载时抖动一轮,故意空依赖数组(同 breathing-field 的既有先例)。
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, []);

  if (word.state === "gone" && display === "") return null;
  return (
    <span className={word.state === "intact" ? "text-white" : "text-white/35"}>
      {display}
    </span>
  );
}

export default function DreamDecay() {
  const [phase, setPhase] = useState<"loading" | "empty" | "written">("loading");
  const [record, setRecord] = useState<StoredRecord | null>(null);
  const [changedIndices, setChangedIndices] = useState<Set<number>>(new Set());
  const [draft, setDraft] = useState("");

  // 挂载 = 一次复述(如果已有记录)。ssr:false 下这是首次真正执行的地方,
  // 避免"先闪一下写作提示、再闪成已有的梦"这种状态跳变(不是水合问题,
  // 是纯粹的体验问题——ssr:false 组件本就不参与服务端渲染)。
  useEffect(() => {
    const existing = loadRecord();
    if (!existing) {
      setPhase("empty");
      return;
    }
    const decayed = decayOnce(existing.words, existing.anchorIndex);
    const changed = new Set<number>();
    decayed.forEach((w, i) => {
      if (w.state !== existing.words[i].state) changed.add(i);
    });
    const next: StoredRecord = {
      ...existing,
      words: decayed,
      decayCount: existing.decayCount + 1,
    };
    saveRecord(next);
    setRecord(next);
    setChangedIndices(changed);
    setPhase("written");
  }, []);

  const submit = () => {
    const trimmed = draft.trim();
    if (!trimmed) return;
    const tokens = tokenize(trimmed);
    const words: DecayWord[] = tokens.map((t) => ({ ...t, state: "intact" }));
    const next: StoredRecord = {
      words,
      decayCount: 0,
      anchorIndex: firstWordIndex(tokens),
    };
    saveRecord(next);
    setRecord(next);
    setChangedIndices(new Set());
    setPhase("written");
  };

  if (phase === "loading") return null;

  return (
    <div className="absolute inset-0 flex items-center justify-center overflow-hidden bg-black p-8">
      {phase === "empty" && (
        <div className="w-full max-w-xl">
          <textarea
            value={draft}
            onChange={(e) => setDraft(e.target.value)}
            placeholder="write down a dream…"
            rows={5}
            className="w-full resize-none border border-white/10 bg-white/[0.02] p-4 font-sans text-white placeholder:text-white/30 focus-visible:border-green-500/40 focus-visible:outline-none"
          />
          <p className="mt-3 font-mono text-xs text-white/40">
            stored only in this browser · never uploaded · will not stay exact
          </p>
          <button
            type="button"
            onClick={submit}
            className="mt-4 font-mono text-xs text-white/60 underline-offset-4 transition-colors duration-200 hover:text-white focus-visible:text-white"
          >
            let it go →
          </button>
        </div>
      )}

      {phase === "written" && record && (
        <div className="w-full max-w-xl">
          <p className="font-sans text-lg leading-relaxed">
            {record.words.map((w, i) =>
              w.isWord ? (
                <DecayWordSpan key={i} word={w} justChanged={changedIndices.has(i)} />
              ) : (
                <span key={i}>{w.text}</span>
              ),
            )}
          </p>
          <p className="mt-6 font-mono text-xs text-white/40">
            reread {record.decayCount} time{record.decayCount === 1 ? "" : "s"}
          </p>
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 2: 写 `public/lab/003-dream-decay.svg`(静态海报,波形向右侧淡出暗示消散)**

```svg
<svg xmlns="http://www.w3.org/2000/svg" width="800" height="450" viewBox="0 0 800 450" role="img" aria-label="dream decay">
  <rect width="800" height="450" fill="#000"/>
  <defs>
    <filter id="b3" x="-20%" y="-20%" width="140%" height="140%">
      <feGaussianBlur stdDeviation="7"/>
    </filter>
    <linearGradient id="fade3" x1="0" y1="0" x2="1" y2="0">
      <stop offset="0%" stop-color="#22c55e" stop-opacity="0.55"/>
      <stop offset="65%" stop-color="#22c55e" stop-opacity="0.25"/>
      <stop offset="100%" stop-color="#22c55e" stop-opacity="0.03"/>
    </linearGradient>
  </defs>
  <g filter="url(#b3)" fill="none" stroke-linecap="round">
    <path d="M0 210 C 100 180, 200 240, 300 210 S 480 170, 560 210" stroke="url(#fade3)" stroke-width="26"/>
    <path d="M0 235 C 120 260, 240 200, 360 235 S 560 270, 640 240" stroke="#4ade80" stroke-width="18" opacity="0.28"/>
    <path d="M0 190 C 140 210, 280 170, 420 195" stroke="#16a34a" stroke-width="20" opacity="0.22"/>
  </g>
</svg>
```

同一视觉家族(黑底 + 模糊绿色波形),但线条在画面右侧提前收尾、渐隐——静态海报里就先暗示"没铺满、正在消失",呼应主题,不需要额外文字说明。

- [ ] **Step 3: 在 `lib/experiments.ts` 新增注册**

```typescript
const DreamDecay = dynamic(() => import("@/components/experiments/dream-decay"), {
  ssr: false,
});

const REGISTRY: Record<string, ComponentType> = {
  "001-breathing-field": BreathingField,
  "002-tonight-tides": TonightTides,
  "003-dream-decay": DreamDecay,
};
```

- [ ] **Step 4: 类型检查 + 构建 + 手动验证**

Run: `npx tsc --noEmit`
Expected: 无错误。
Run: `pnpm build:willsleep`
Expected: 构建成功。
Run: `pnpm dev` → 打开 `http://localhost:3000/lab/003-dream-decay/`
Expected:
1. 首次访问:居中一个 textarea,提示"write down a dream…",下方一行 mono 隐私条款,`let it go →` 提交。
2. 输入一段中英混排文字(比如"我 dreamed 走进了一条 hallway"),提交后原样清晰显示,底部 `reread 0 times`。
3. 刷新页面(重新进入 = 一次复述):约 15% 的词先抖动几帧乱码字符,定格为乱码(变暗,`text-white/35`);`reread 1 times` → 语法上应为 `reread 1 time`(单数),核对 Step 1 代码里的三元表达式在 `decayCount===1` 时不加 `s`,若手滑打成 times 在这一步改掉。
4. 再刷新几次:更多词变乱码;已经是乱码的词里,部分会在某次复述里直接消失(不占位,句子视觉上变短);永远留一个词(锚点,通常是句首第一个词)不受影响。
5. 连续刷新到下限:整体只剩约 20% 原词仍然可读(乱码或原文),不会归零。
6. DevTools → Application → Local Storage:能看到 `sl.exp003.dream` 键,内容随每次刷新变化;Network 面板全程零请求。
7. 系统开"减弱动态效果"→ 抖动动画降级为瞬时替换,衰减结果不变(信息零损失,只是过程不演)。
8. 左上徽标展开四栏面板,observation 栏是"遗忘本身不发出任何声音"那句话原文。

- [ ] **Step 5: Commit**

```bash
git add components/experiments/dream-decay.tsx public/lab/003-dream-decay.svg lib/experiments.ts
git commit -m "feat(lab): EXP-003 dream decay — localStorage-only forgetting, two-stage word decay"
```

---

## Task 4: 验收与收尾

**Files:** 无新改动;仅验证 + 视手动走查结果做小修。

- [ ] **Step 1: 双域名构建**

Run: `npx tsc --noEmit`
Expected: 无错误。
Run: `pnpm build:willsleep`
Expected: 构建成功;`out/lab/002-tonight-tides/index.html` 与 `out/lab/003-dream-decay/index.html` 均存在;`out/lab/index.html` 里三张卡片齐全。
Run: `pnpm build:yueqiao`
Expected: 构建成功;`out/` 里没有任何 `/lab/` 可达页面(路由层的门控是 Phase 2 已验收的,本期没碰 `app/lab/[slug]/page.tsx`,理论上不会退化,但仍要跑一遍确认没有引入意外依赖)。

- [ ] **Step 2: /lab 索引走查**

Run: `pnpm dev` → 打开 `http://localhost:3000/lab/`
Expected: Focus Cards 网格三张卡片(001/002/003),海报图齐全;hover 任一张,其余暗化(签名效果对新卡片同样生效,因为 `lab-grid.tsx` 本期零改动、天然通用);走廊 `/` 页 hover `lab` 门牌,门缝读数变为 `lab: 3 experiments · 3 ongoing`。

- [ ] **Step 3: 策展三问自查(逐条写进 commit 前的自检,仿 Phase 2 Task 6 的红线自查格式)**

**EXP-002 tonight's tides:**
- ① 它在研究一个问题吗?——今晚几点醒,最不难受?是问句。✅
- ② 拿走 simplex-noise 与场绘制手法,剩下什么?——90 分钟睡眠结构的参数化方式(深浅比例逐周期变化)、浅睡窗口的区间表示法(非单点)、诚实条款本身。✅
- ③ 它的动画在传达信息吗?——漂移 = 潮汐持续流动;高亮区间的位置与宽度 = 浅睡窗口,不是装饰。✅

**EXP-003 dream decay:**
- ① 它在研究一个问题吗?——梦是在哪一次复述里丢掉的?是问句,且需要多次复访才能验证。✅
- ② 拿走 vanish input 与字符抖动手法,剩下什么?——衰减算法本身(选词方式、下限、锚点保留、两段式渐进)、"复访即代价"的机制设计、localStorage-only 的诚实条款。✅
- ③ 它的动画在传达信息吗?——字符抖动显示的正是"哪些词刚刚丢了",是测量的可视化。✅

- [ ] **Step 4: 声音与红线自查**

- 声音三法则:两实验全程静音;`lib/audio.ts` 零改动;DevTools Network 面板在两实验页均无音频请求。✅
- 红线 1(无简历内容):本期没引入任何简历式内容。✅
- 红线 2(career 入口只有页脚小字):未新增 career 入口。✅
- 红线 3(宁缺毋滥):两实验都是真实可用的仪器,不是占位。✅
- 仪表读数一致性:`bedtime`、醒来时间标签、`reread N times` 与其它房间的读数并排截图应分不出出处(§DESIGN 2)。

- [ ] **Step 5(如走查中有小修改): Commit**

```bash
git add -A
git commit -m "fix(lab): EXP-002/003 polish from manual walkthrough"
```

若走查全部通过、无需修改,跳过本步,Task 3 的两个 commit 已是本期终态。

---

## 收尾说明

本期不涉及 `lib/rooms.ts`(lab 门牌早已 `open: true`)、不涉及 `app/sitemap.ts`(已通用遍历 `getLabExperiments()`,新实验自动进 sitemap)、不涉及 `lib/i18n/strings.ts`(两实验的仪表读数全部按 §7.1 硬编码英文)。这也是"本期壳层零改动"在实现层面的完整验证——如果 Task 1–3 执行完发现必须碰以上任一文件,先停下来看是不是哪里没有复用到 Phase 2 已经建好的通用机制,而不是想当然地改。

**候补方向提醒(不进本期,仅供下一刀参考):** EXP-002 的声化(周期深度 → 滤波截止)与 EXP-003 之外的"遗忘"叙事扩展(接入未来 /notes 的 REM observatory)都明确列为候补,不在这份计划的范围内。
